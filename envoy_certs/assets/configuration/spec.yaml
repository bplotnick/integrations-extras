name: envoy_certs
files:
- name: envoy_certs.yaml
  options:
  - template: init_config
    options:
    - template: init_config/default
  - template: instances
    options:
    - template: instances/default
